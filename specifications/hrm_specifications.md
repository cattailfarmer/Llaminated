# HJM Specifications

This document outlines the high-level reasoning module (HRM) for LLaminated. It is designed to act as a superstructure above the existing LLM core, and not to interfere with its low-level operations.

The module provides an octagonal lattice as a high-level reasoning fabric.

```go